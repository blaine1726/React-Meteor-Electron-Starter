*** Starter package for a meteor-react-electron application ***
*Uses React-coffee in order to have nicer looking code
*Uses Stylus sheets in for more robust CSS
*share components within Meteor are still in beta

*** Running the Application (w/in directory) ***
* with electron *
electrify
* without electron *
meteor